{
  // TEAM CONFIGURATION - maintained centrally, do not edit.
  //
  // This repository is a preconfigured setup for our team, not a general-
  // purpose package: this file already declares the team's provider, models,
  // permissions and defaults. Changes go through a reviewed commit here, so
  // every sandbox stays identical.
  //
  // How ocbox uses it: mounted into every sandbox as OpenCode's *global*
  // config, and in --no-sandbox mode placed in the config directory ocbox
  // hands OpenCode through OPENCODE_CONFIG_DIR.
  //
  // provider.local.options.baseURL depends on the mode:
  //   - web/--tui (sandboxed): ocbox overrides it to point at the relay
  //     reaching the LLM - the value here is ignored.
  //   - --no-sandbox: used as-is, so it must be the LLM server's real,
  //     directly-reachable address.
  //
  // Verified against opencode 1.18.29: every key below survives
  // `opencode debug config`. OpenCode drops keys it doesn't recognise
  // silently, so check any key added the same way.
  //
  // Standing instructions do NOT belong here - OpenCode's `instructions` key
  // is parsed but never resolved. Team rules go in opencode-config/AGENTS.md.

  "$schema": "https://opencode.ai/config.json",
  "lsp": true,
  // Disable session sharing
  "share": "disabled",
  // Disable OpenTelemetry telemetry
  "experimental": {
    "openTelemetry": false
  },
  "compaction": {
    "auto": true,
    "prune": false,
    "reserved": 10000
  },
  "attachment": {
    "image": {
      "auto_resize": true,
      "max_width": 2000,
      "max_height": 2000,
      "max_base64_bytes": 5242880
    }
  },
  "provider": {
    "local": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "ai-lpec (local)",
      "options": {
        "baseURL": "http://<IP>:<PORT>/v1"
      },
      "models": {
        "Qwen3.8-27B-smart": {
          "name": "Qwen3.8-27B-smart",
          "modalities": {
            "input": ["text", "image"],
            "output": ["text"]
          },
          "limit": {
            "context": 200000,
            "output": 131072
          }
        },
        // NOTE: this model's key says "medium" but its "name" says
        // "smart-think" - double check which one was intended.
        "Qwen3.8-27B-medium": {
          "name": "Qwen3.8-27B-smart-think",
          "modalities": {
            "input": ["text", "image"],
            "output": ["text"]
          },
          "limit": {
            "context": 200000,
            "output": 131072
          }
        },
        "smart-glm": {
          "name": "Glm5.3",
          "limit": {
            "context": 200000,
            "output": 81920
          }
        },
        "Qwen3.6-35B-fast": {
          "name": "Qwen3.6-35B-fast",
          "modalities": {
            "input": ["text", "image"],
            "output": ["text"]
          },
          "limit": {
            "context": 200000,
            "output": 81920
          }
        },
        "Qwen3.6-35B-fast-think": {
          "name": "Qwen3.6-35B-fast-think",
          "modalities": {
            "input": ["text", "image"],
            "output": ["text"]
          },
          "limit": {
            "context": 200000,
            "output": 81920
          }
        }
      }
    }
  },
  "small_model": "local/Qwen3.6-35B-fast",
  "disabled_providers": [
    "openai", "anthropic", "google", "azure", "bedrock",
    "cohere", "fireworks", "groq", "mistral", "perplexity",
    "together", "xai", "deepseek", "huggingface", "opencode"
  ],
  "permission": {
    "external_directory": {
      "*": "deny",
      "~/.config/opencode/skills/code-review/*": "allow",
      "~/.config/opencode/skills/sota-review/*": "allow"
    },
    "doom_loop": "deny"

    // Lecture seule dans le working directory
    //"read": "allow",
    // Interdire toute modification de fichiers
    //"edit": "deny",
    // Autoriser uniquement les commandes git
    //"bash": {
    //  "*": "deny",
    //  "git *": "allow"
    //},
    // Autoriser la recherche dans le code (utile pour la lecture)
    //"grep": "allow",
    // Autoriser le glob pour lister les fichiers
    //"glob": "allow",
    // Interdire l'accès aux répertoires externes
    // Interdire la navigation web
    // "webfetch": "deny",
    // Interdire la recherche web
    // "websearch": "deny"
    // Interdire les questions utilisateur (optionnel)
    //"question": "deny"
  }
}
